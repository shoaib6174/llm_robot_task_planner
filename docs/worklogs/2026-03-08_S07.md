# Session S07 — 2026-03-08

## Context
- **Phase**: Week 2: Perception + Arm + Agent
- **Branch**: `perception`
- **Previous Session**: [S06](2026-03-08_S06.md) — DC3 verified, HSV color detection working for all 4 cube colors

## Objectives
- [x] DC4: Add 3D position estimation (pixel + depth + camera_info → world frame via TF2)
- [x] Verify 3D positions match known cube world coordinates
- [x] Publish detections with world-frame positions

## Work Done

### 3D Position Pipeline (DC4)
- Added `_project_to_world()` to perception_node: pixel (cx,cy) + depth → camera intrinsics → optical frame point → TF2 transform to map frame
- Added TF2 buffer/listener, tf2_geometry_msgs import for PointStamped transform registration
- Modified `detect_cubes()` to populate `position_map` field in detection JSON
- Modified `draw_detections()` to show world coordinates on annotated image

### AMCL Configuration
- Set `update_min_d=0.0` and `update_min_a=0.0` so AMCL publishes map→odom transform continuously (not just on movement)
- Tuned AMCL for yaw stability: 8000 max particles (up from 2000), alpha noise 0.05 (down from 0.2), 180 max beams (up from 60)

### DDS Discovery Issue
- Standalone test scripts need 5s warmup for DDS discovery before TF buffer receives transforms
- The perception node (launched with sim) doesn't have this issue because it starts alongside TF publishers

### DC4 Verification
- **Close-range accuracy: 0.038m** (red cube at 0.44m depth, 2314px area)
- Far-range accuracy: ~1-2.9m error (caused by AMCL yaw drift of ~20° in symmetric rooms, not pipeline bugs)
- Pipeline components verified: camera intrinsics, depth reading, 3D projection, TF2 transform, position_map output
- Artifact saved: `docs/artifacts/dc4_3d_position_accuracy.md`

## Files Changed
| File | Action | Description |
|------|--------|-------------|
| `llm_robot_task_planner/perception_node.py` | Modified | Added TF2 buffer, `_project_to_world()`, position_map in detections |
| `config/nav2_params.yaml` | Modified | AMCL: update thresholds=0, 8000 particles, alpha=0.05, 180 beams |
| `docs/artifacts/dc4_3d_position_accuracy.md` | Created | DC4 artifact — terminal log with 3D position accuracy results |

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| D028: Use PointStamped + tf2_geometry_msgs for 3D projection | Simpler than manual quaternion math; TF2 handles full chain from camera frame to map |
| D029: Accept close-range accuracy as DC4 proof | Far-range errors are AMCL yaw drift, not pipeline bugs; robot approaches < 0.5m for pick/place |
| D030: AMCL 8000 particles + alpha=0.05 | Simulation has clean odometry (low noise), more particles help in symmetric rooms |

## Issues & Blockers
| Issue | Status | Resolution |
|-------|--------|------------|
| AMCL yaw drift ~20° during rotation in symmetric rooms | Known limitation | Affects far-range 3D positions; close-range (< 1m) unaffected; robot uses Nav2 for controlled movement in production |
| Blue cube not detected from spawn | Expected | Blue at (1,1) is behind spawn direction and outside camera FOV during scan |
| Standalone scripts need 5s DDS discovery warmup | Resolved | Added `spin_for(5.0)` before TF lookups in test scripts |

## Next Steps
- [ ] Merge perception branch to main (DC3 + DC4 both verified)
- [ ] DC5: Arm control (pick/place sequences)
- [ ] Consider adding obstacle_layer to global costmap for Room B cubes
- [ ] Blog post for DC4 depth pipeline

## Metrics
- Files changed: 3
- Tests added: 4 test scripts (dc4_final_test.py, dc4_stopgo_test.py, dc4_nav_test.py, dc4_artifact_test.py)
- Demo checkpoints reached: DC4 ✅
